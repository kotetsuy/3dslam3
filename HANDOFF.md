# HANDOFF: 約4枚の写真から約10秒で3Dモデルを作る技術の選定

作成日: 2026-09-21

## ユーザーの目的

`/home/your-user/VGGT/` と `/home/your-user/room3dgs/` で3D再構成を試したが、品質に満足できなかった。約4枚の写真から約10秒で3Dモデルを作成するための技術候補を比較したい。

今回実施したのは既存記録の確認と公開資料の調査のみ。新しいモデルのインストール、実行、速度・品質の検証は行っていない。既存フォルダも変更していない。参照文書内の作業指示はユーザーの依頼とは区別し、実行していない。

## 推奨方針

最初の比較対象は、物体なら **FreeSplatter-O**、部屋・風景なら **Depth Anything 3（DA3）**。

少数写真からの一括再構成として検討する。従来型SLAMの入れ替えだけでは、入力情報の不足や後段処理の時間は解決しない。また、room3dgsは既に一括推論型のWorldMirrorを使っていたため、一括推論に変更すること自体は品質改善の根拠にならない。

出力の用途を分ける必要がある。

- 視点を動かして眺めたい: 3D Gaussian Splatting（3DGS）が候補。
- 編集・形状処理に使いたい: メッシュが必要。3DGSの生成完了とメッシュの完成は同じではない。
- 寸法測定に使いたい: スケールと幾何精度の検証が別途必要。
- 見えない裏面も埋めたい: AIによる推測を許容するか決める。未撮影部分の正確な復元は保証できない。

## 既存実験から分かったこと

以下は今回の再測定ではなく、各フォルダの記録に基づく。

### VGGT / VGGS

参照: `/home/your-user/VGGT/HANDOFF.md`、特に末尾の「step 1 の時間内訳を実測」。

記録上の環境はRadeon 8060S / gfx1151。ヘッドホン10枚、1200×1600、`--use_ba --query_frame_num 0 --max_query_pts 4096` の計測では次の内訳だった。

| 処理 | fine_tracking ON | OFF |
|---|---:|---:|
| VGGT backbone | 6.4秒 | 6.1秒 |
| 特徴点追跡 `predict_tracks` | 563.7秒 | 66.4秒 |
| 三角測量 | 0.3秒 | 0.2秒 |
| Bundle Adjustment（BA） | 0.4秒 | 0.4秒 |
| 重みロード・画像IO等 | 162.8秒 | 163.0秒 |
| 合計 | 733.4秒 | 236.2秒 |

**初期記録の「ほぼ全部がBA」という説明は、末尾の実測で訂正されている。主な負荷は追跡とロード等。** この値は上記設定に依存する。

- 既存構成にはVGGTを2回実行する重複がある。
- 後段のVGGS学習は別途、ヘッドホン10枚・3000反復で10分22秒。
- 渋谷駅では、信頼度が平坦なVGGTの深度等を事前分布として使うと学習結果が悪化した記録がある。
- 現行のVGGS経路から単純にBA付き処理を外すと、初期点の生成条件を満たさなくなる問題が記録されている。高速化する場合は、直接出力する別経路として設計する必要がある。

### room3dgs / WorldMirror

参照: `/home/your-user/room3dgs/PROGRESS.md`、`TECHNICALJ.md`、`PHASE0_RESULT.md`。

- WorldMirror v1.1による一括推論で3DGSを生成。
- 室内サンプル8枚では、モデルウォーム・入力が518px相当の条件で推論12.3秒、全体約30秒。
- 別条件の初期実測は約182秒であり、同じ条件の速度として比較しない。
- 実写の渋谷駅8枚では推論114秒。ヘッドホン10枚も試したが、両方とも品質不足と記録されている。
- 当時の環境ではgsplatのwave幅に関するビルド問題やGPUハングが記録されている。これを現在のすべてのAMD実装に一般化しない。

速度は処理構成、品質はモデルの適性・撮影条件・写真間の重なりを分けて評価する。

## 技術候補

| 候補 | 適した用途と出力 | 約10秒への見込み | 主な条件・制約 |
|---|---|---|---|
| FreeSplatter-O / S | Oは物体、Sは部屋・風景。写真群から直接3DGS | 有望。公式は数秒での再構成を報告 | 8060Sで未検証。通常の3DGSは編集用メッシュではない |
| Depth Anything 3 | 部屋・風景の深度・姿勢・点群。対応モデルでは3DGS | 有望だがモデルと出力次第 | 小型の深度・姿勢モデルと3DGS対応モデルを混同しない |
| NoPoSplat | 少数視点の部屋・風景を3DGS化し、新しい視点を描画 | 速度重視の比較候補 | 公開重みは主に2枚用と3枚用。4枚をそのまま入力できる前提にしない |
| InstantMesh | 物体の形を補完してメッシュ生成 | 論文は10秒以内を報告 | 標準構成は1枚から別視点画像を生成。実写4枚の忠実な復元とは異なる |

これらは選定上の見込みであり、ユーザー環境での時間保証ではない。WorldMirrorより今回の写真で高品質になるかも未検証。

### 1. FreeSplatter: 物体向けの第一候補

- 物体用FreeSplatter-Oとシーン用FreeSplatter-Sがある。
- 物体の表面表現を検討する場合はFreeSplatter-O-2dgsも候補。
- 細い部品、反射、未撮影の裏面などの再現は個別に評価する。
- 公式PyTorch実装の推奨環境はCUDA中心であり、ROCm対応は実測が必要。

第三者実装 **free-splatter.cpp** はGGMLとVulkanを使用するため、AMD環境でROCm以外の経路を検証する候補になる。作者による計測はRTX 5070 Ti・512×512の2枚でforward 0.22秒。これは8060Sでの速度でも、ロード・出力・表示込みの時間でもない。移植実装の出力品質も確認する必要がある。

資料:
- https://github.com/TencentARC/FreeSplatter
- https://arxiv.org/abs/2412.09573
- https://github.com/localai-org/free-splatter.cpp

### 2. DA3: 部屋・風景向けの第一候補

- 複数画像から整合する深度・カメラ姿勢を推定する。
- 著者らのベンチマークではVGGTを上回るが、今回の写真での改善を保証するものではない。
- まず点群で壁・床・物体の位置関係を比較し、形状が改善したら3DGS出力を評価する。
- 公式APIではGaussian出力はDA3-GIANT / DA3NESTED-GIANT-LARGEに対応。モデル選定時に最新の対応表を再確認する。
- メッシュ化や表示処理を加えた総時間は別途測定する。

資料:
- https://github.com/ByteDance-Seed/Depth-Anything-3
- https://github.com/ByteDance-Seed/Depth-Anything-3/blob/main/docs/API.md
- https://arxiv.org/abs/2511.10647

### 3. NoPoSplat: 少数視点のシーン描画の比較候補

カメラ姿勢が未知の少数画像から3DGSを推定する。公式公開重みの入力枚数は2枚または3枚なので、「4枚程度」という条件で2〜3枚も許容する場合の候補。部屋全体の完全なメッシュ化より、入力付近の視点での見た目を評価する用途に向く。

資料: https://github.com/cvg/NoPoSplat

### 4. InstantMesh: 推測を許容して完成した物体メッシュが欲しい場合

標準構成は単一画像から複数視点画像を生成し、メッシュへ再構成する。実物の裏面・寸法を正確に復元するより、ゲーム用素材や概形の生成に適する。公開コードの4視点オプションを、任意の実写4枚をそのまま復元する機能と解釈しない。

資料:
- https://github.com/TencentARC/InstantMesh
- https://arxiv.org/abs/2404.07191

## 次の比較実験案（未実施）

1. 物体と部屋を分け、それぞれ同じ4枚を用意する。NoPoSplatは対応する2〜3枚の条件を明記する。
2. 比較基準として既存結果を残し、物体はFreeSplatter-O、部屋はDA3を試す。
3. モデルを常駐させ、初回起動とウォーム実行を分ける。
4. 「画像投入から初回3D表示まで」を約10秒の判定対象とする。前処理・推論・出力変換・読み込み・表示を含め、内訳も記録する。
5. 各画像の解像度、モデル、精度、デバイス、出力点数を記録する。
6. 品質は、形状の二重化、浮遊点、穴、細部、入力間の視点移動での破綻を確認する。可能なら入力に使わない写真と比較する。
7. メッシュが必要なら、その生成時間と品質も独立して記録する。

撮影は、同じ領域が複数枚に重なるように、カメラ位置を変えて行う。部屋の別方向を1枚ずつ撮るだけでは共通の観測が不足しやすい。4枚で部屋全体や物体全周を正確に復元することを前提にしない。

## 未確定事項

- 最優先の対象は物体か、部屋・風景か。
- 必要な成果物は3DGS、点群、編集用メッシュのどれか。
- 裏面のAI補完や実物との形状差をどこまで許容するか。
- 約10秒に初回モデルロードを含めるか。
- 8060Sでのローカル処理が必須か。

調査時点で新規実装の依頼はない。次の実装に進む場合は、上記の用途と時間の定義を踏まえて評価する。
